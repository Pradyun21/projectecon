import type { Metadata } from "next";
import Link from "next/link";
import { EmojiGlyph } from "../components/EmojiGlyph";
import { PageIntro, Reveal } from "../components/SiteShell";
import { projects } from "../data/projects";
export const metadata:Metadata={title:"Projects",description:"See how Project Econ applies economic analysis to real-world business problems."};
export default function Work(){return <><PageIntro eyebrow="Projects" title="Economic analysis, clearly documented." text="Each completed project will demonstrate how economics can address a real-world business problem."/><section className="section work-section"><div className="container">{projects.length===0?<Reveal className="empty-card work-empty"><div className="empty-icon"><EmojiGlyph emoji="📁"/></div><p className="eyebrow">The first chapter</p><h2>Projects coming soon.</h2><p>Each project will document the business challenge, economic concept applied, analysis performed, recommendation, implementation, measurable outcomes, and lessons learned.</p><p>Project Econ is currently building community partnerships and preparing for its first business assessments in Cabarrus County.</p><Link className="button" href="/work-with-us">Discuss a business challenge <EmojiGlyph emoji="➡️"/></Link></Reveal>:<div className="project-grid">{projects.map(p=><article className="project-card" key={p.name}><p className="eyebrow">{p.date}</p><h2>{p.name}</h2><p>{p.problem}</p><strong>{p.concept}</strong></article>)}</div>}</div></section></>}
