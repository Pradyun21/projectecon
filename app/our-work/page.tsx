import type { Metadata } from "next";
import { ArrowRight, FolderOpen } from "lucide-react";
import Link from "next/link";
import { CallToAction, PageIntro, Reveal } from "../components/SiteShell";
import { projects } from "../data/projects";
export const metadata:Metadata={title:"Our Work",description:"See Project Econ case studies and the economics behind each local project."};
export default function Work(){return <><PageIntro eyebrow="Our work" title="Small experiments. Clearly documented." text="Every completed project will show the problem, concept, strategy, implementation, results, and what we learned."/><section className="section"><div className="container">{projects.length===0?<Reveal className="empty-card work-empty"><div className="empty-icon"><FolderOpen/></div><p className="eyebrow">The first chapter</p><h2>Projects coming soon.</h2><p>Project Econ is currently looking for its first businesses and organizations in Cabarrus County. Once we complete a project, we will explain the problem, economic concept, strategy, implementation, and results on this page.</p><Link className="button" href="/contact">Be one of the first <ArrowRight size={18}/></Link></Reveal>:<div className="project-grid">{projects.map(p=><article className="project-card" key={p.name}><p className="eyebrow">{p.date}</p><h2>{p.name}</h2><p>{p.problem}</p><strong>{p.concept}</strong></article>)}</div>}</div></section><CallToAction title="Become an early Project Econ partner." text="If your organization has a challenge involving customers, participation, pricing, decisions, or incentives, we’d like to hear about it."/></>}
